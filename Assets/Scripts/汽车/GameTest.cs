
using UnityEngine;
using static UnityEditor.ObjectChangeEventStream;


public class GamneTest : MonoBehaviour
{
    //public CarData carData;


    /*private void Start()
     {
         //创建具体建造者
         CarBuilder builder = new X5CarBuilder(carData);
         //创建指挥者并传入建造者
         CarDirector director = new CarDirector(builder);
         //指挥者控制建造过程
         director.ConstructCar();
         //获取建造好的汽车
         Car car = director.GetCar();
         //将汽车放置在场景中的指定位置
         car.CarObject.transform.position = new Vector3(0, 2, 0);

     }*/
   

}
